{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Account Closure (response)",
    "description": "The result of account closure request made.",
    "type": "object",
    "required": [
        "echo_req",
        "msg_type"
    ],
    "properties": {
        "account_closure": {
            "title": "account_closure",
            "description": "If set to 1, all accounts are closed.",
            "type": "integer",
            "enum": [
                0,
                1
            ]
        },
        "echo_req": {
            "description": "Echo of the request made.",
            "type": "object"
        },
        "msg_type": {
            "description": "Action name of the request made.",
            "type": "string",
            "enum": [
                "account_closure"
            ]
        },
        "req_id": {
            "description": "Optional field sent in request to map to response, present only when request contains req_id.",
            "type": "integer"
        }
    }
}
