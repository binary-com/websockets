{
  "type": "object",
  "title":"Pricing table Response",
  "description":"Latest prices for a specific contracts' table",
  "properties": {
    "echo_req": {
      "title":"Echo request",
      "description":"Echo of the request made"
    },
    "pricing_table": {
      "type": "object",
      "title":"Pricing table Response",
      "description":"Latest prices for a specific contracts' table",
      "properties": {
        "prices":{
          "type": "object",
          "title": "Prices for table",
          "description": "List of prices for all pre-set barriers"
        }
      }
    },
    "msg_type": {
      "type": "string",
      "description": "pricing_table"
    },
    "req_id": {
      "type": "integer",
      "description": "Optional field send in request to map to response, present only when request contains req_id"
    }
  },
  "required": [
    "echo_req",
    "pricing_table",
    "msg_type"
  ]
}
